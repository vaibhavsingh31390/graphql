{
  "name": "graphql_starter",
  "private": true,
  "scripts": {
    "dev": "npm run --workspace=server start & npm run --workspace=client watch",
    "start": "npm run --workspace=server start & npm run --workspace=client build"
  },
  "workspaces": [
    "server",
    "client"
  ]
}
